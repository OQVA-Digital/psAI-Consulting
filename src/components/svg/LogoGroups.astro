---

const { group } = Astro.props;

---

<style>
    path{
        fill:currentColor;
        stroke-width:0px;
    }
</style>

{group == "group" ? (  
    <span class="psai logo">ps<b>AI</b>
        <span class="group">group</span>
    </span>
) : ""}

{group == "consulting" ? (
    <span class="psai logo">ps<b>AI</b>
        <span class="group">consulting</span>
    </span>
) : ""}

{group == "dev" ? (
    <span class="psai logo">ps<b>AI</b>
        <span class="group">dev</span>
    </span>
) : ""}

{group == "labs" ? (
    <span class="psai logo">ps<b>AI</b>
        <span class="group">labs</span>
    </span>
) : ""}

{group == "events" ? (
    <span class="psai logo">ps<b>AI</b>
        <span class="group">events</span>
    </span>
) : ""}

{group == "foundation" ? (
    <span class="psai logo">ps<b>AI</b>
        <span class="group">foundation</span>
    </span>
) : ""}

{group == "education" ? (
    <span class="psai logo">ps<b>AI</b>
        <span class="group">education</span>
    </span>
) : ""}